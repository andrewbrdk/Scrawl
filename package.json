{
  "dependencies": {
    "@types/katex": "^0.16.8",
    "katex": "^0.16.45",
    "quill": "^2.0.3"
  },
  "devDependencies": {
    "@types/quill": "^2.0.14",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild app.ts --bundle --loader:.ttf=file --loader:.woff=file --loader:.woff2=file --loader:.eot=file --loader:.svg=file --outfile=dist/app.js && cp index.html style.css dist/"
  }
}

{
  "dependencies": {
    "quill": "^2.0.3"
  },
  "devDependencies": {
    "@types/quill": "^2.0.14",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild app.ts --bundle --outfile=dist/app.js && cp index.html style.css dist/"
  }
}
